#include "NetworkClient.h"
#include <iostream>
using namespace std;
int main()
{
  int sock;
  sock = socket(AF_INET,SOCK_STREAM,0);
  sockaddr_in addr;
  //create_addr(addr, "1.1.1.1", 4444);
  Connect(addr, sock, "localhost", 6000);
  close(sock);
  cout<<"hhhh"<<endl;
  return 0;
}
